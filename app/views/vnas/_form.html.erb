<%= form_for(vna) do |f| %>
  <% if vna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vna.errors.count, "error") %> prohibited this vna from being saved:</h2>

      <ul>
      <% vna.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
